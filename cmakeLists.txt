cmake_minimum_required(VERSION 3.13)

project(fitness-pico-workspace NONE)

# Workspace wrapper:
# Route top-level CMake users into the real Pico project without creating
# a second SDK download/build flow via ExternalProject.
add_subdirectory(Fit-pico ${CMAKE_BINARY_DIR}/fitpico)
